# Run `hyprctl monitors` after first login to get your exact output name
# (e.g. eDP-1, DP-1, HDMI-A-1) and replace the wildcard below if you want
# per-monitor control. The wildcard works fine for a single 1920x1080@60 setup.

monitor = , 1920x1080@60, auto, 1
